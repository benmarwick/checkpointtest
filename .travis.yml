# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages
warnings_are_errors: false

install:
 - R -e "install.packages('checkpoint', repos = 'https://cran.rstudio.com'
 - R -e "checkpoint::checkpoint(2016-09-02)"
